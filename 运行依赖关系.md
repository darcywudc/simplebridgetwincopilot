# 桥梁结构分析系统 - 运行依赖关系

## 📋 概述

`main_enhanced_fixed.py` 是桥梁结构分析系统的主程序，它需要以下程序文件才能正常运行：

---

## 🎯 **核心依赖文件**

### 1️⃣ **主要桥梁模型模块**

#### `bridge_model_enhanced.py` ⭐ **最重要**
- **功能**: 增强版桥梁模型，基于 xara/OpenSees
- **类**: `BridgeModelXara`
- **作用**: 
  - 专业有限元分析引擎
  - 支持可配置梁刚度、支座类型、墩台高度
  - 精确计算弯矩、剪力、支座反力
  - 1mm精度的墩台高度效应模拟

#### `bridge_model.py` 
- **功能**: 简化版桥梁模型
- **类**: `BridgeModel`
- **作用**: 
  - 快速分析引擎（备用）
  - 基于自制有限元
  - Mac系统兼容性好

#### `simple_fea.py`
- **功能**: 自制有限元分析引擎
- **类**: `SimplifiedFEA`
- **作用**:
  - 为 `bridge_model.py` 提供计算支持
  - 梁单元刚度矩阵组装
  - 求解线性方程组

### 2️⃣ **可视化模块**

#### `visualization_enhanced.py` ⭐ **重要**
- **功能**: 增强版可视化模块
- **类**: `BridgeVisualizer`
- **作用**:
  - 生成弯矩图、剪力图
  - 支座反力分布图
  - 3D桥梁可视化
  - Plotly交互式图表

---

## 📦 **Python包依赖**

### 必需的第三方包（requirements.txt）:

```txt
streamlit>=1.28.0     # Web应用框架
numpy>=1.24.0         # 数值计算
pandas>=2.0.0         # 数据处理
matplotlib>=3.7.0     # 基础绘图
plotly>=5.15.0        # 交互式图表
scipy>=1.11.0         # 科学计算
```

### 核心包说明:

1. **streamlit** - Web界面框架
   - 提供用户界面组件
   - 侧边栏配置面板
   - 结果展示标签页

2. **numpy** - 数值计算核心
   - 矩阵运算
   - 数学函数
   - 数组操作

3. **pandas** - 数据分析
   - 表格数据处理
   - 支座反力表格
   - 数据框操作

4. **plotly** - 交互式可视化
   - 弯矩图、剪力图
   - 支座反力分布
   - 3D桥梁模型

5. **matplotlib** - 基础绘图
   - 静态图表生成
   - 图形样式设置

6. **scipy** - 科学计算
   - 稀疏矩阵求解
   - 优化算法

---

## 🔧 **隐藏依赖（自动安装）**

### xara包依赖:
程序使用 `xara` 包作为OpenSees的Python接口：

```python
import xara  # OpenSees Python接口
```

**安装方法:**
```bash
pip install xara
```

---

## 🏗️ **文件结构关系图**

```
main_enhanced_fixed.py (主程序)
├── bridge_model_enhanced.py (专业分析引擎)
│   └── xara (OpenSees接口)
├── bridge_model.py (简化分析引擎) 
│   └── simple_fea.py (自制有限元)
├── visualization_enhanced.py (可视化)
│   ├── plotly.graph_objects
│   └── matplotlib.pyplot
├── streamlit (Web框架)
├── numpy (数值计算)
├── pandas (数据处理)
└── logging (日志记录)
```

---

## 🚀 **完整运行环境搭建**

### Step 1: 确保文件完整性
检查以下文件是否存在：
```
✅ main_enhanced_fixed.py          (主程序)
✅ bridge_model_enhanced.py        (专业分析引擎)
✅ bridge_model.py                 (简化分析引擎)
✅ simple_fea.py                   (自制有限元)
✅ visualization_enhanced.py       (可视化模块)
✅ requirements.txt                (依赖清单)
```

### Step 2: 安装Python依赖
```bash
pip install -r requirements.txt
```

### Step 3: 安装OpenSees接口
```bash
pip install xara
```

### Step 4: 运行程序
```bash
streamlit run main_enhanced_fixed.py
```

---

## ⚠️ **常见问题解决**

### 1. xara包安装失败
```bash
# 方法1: 更新pip
pip install --upgrade pip
pip install xara

# 方法2: 使用conda
conda install -c conda-forge opensees
pip install xara
```

### 2. 模块导入错误
确保所有Python文件在同一目录下，或将目录添加到Python路径：
```python
import sys
sys.path.append('/path/to/your/project')
```

### 3. Streamlit端口冲突
```bash
streamlit run main_enhanced_fixed.py --server.port 8502
```

---

## 🎯 **核心功能模块说明**

### 🏗️ **BridgeModelXara类** (最重要)
- **文件**: `bridge_model_enhanced.py`
- **功能**: 
  - ✅ 支座类型配置 (Fixed Pin / Roller)
  - ✅ 梁刚度配置 (0.5×-2.0×)
  - ✅ 墩台高度配置 (1mm精度)
  - ✅ 强制位移模拟
  - ✅ 支座反力计算
  - ✅ 弯矩剪力提取

### 📊 **BridgeVisualizer类**
- **文件**: `visualization_enhanced.py`
- **功能**:
  - ✅ 弯矩图绘制
  - ✅ 剪力图绘制
  - ✅ 支座反力图
  - ✅ 交互式图表

### 🌐 **Streamlit界面**
- **文件**: `main_enhanced_fixed.py`
- **功能**:
  - ✅ 参数配置界面
  - ✅ 实时计算结果
  - ✅ 多标签页展示
  - ✅ 响应式布局

---

## 📋 **总结**

要运行 `main_enhanced_fixed.py`，您需要：

**必需文件 (4个)**:
1. `bridge_model_enhanced.py` - 专业分析引擎
2. `bridge_model.py` - 简化分析引擎  
3. `simple_fea.py` - 自制有限元
4. `visualization_enhanced.py` - 可视化模块

**必需Python包 (6个)**:
1. `streamlit` - Web框架
2. `numpy` - 数值计算
3. `pandas` - 数据处理
4. `plotly` - 交互式图表
5. `matplotlib` - 基础绘图
6. `scipy` - 科学计算

**可选包 (1个)**:
1. `xara` - OpenSees接口（专业分析需要）

按照上述要求配置环境后，即可成功运行桥梁结构分析系统！ 